# 配图生成记录

## 目标

为 X.com Article 生成一张 `16:9` 主图：`Agent 支付的权限层`。

## 尝试 1：baoyu-image-gen / Tuzi

命令：

```bash
npx -y bun .agents/skills/baoyu-image-gen/scripts/main.ts --promptfiles '.../images/prompts/x-agent-payment-permission-layer.md' --image '.../images/x-agent-payment-permission-layer.png' --preset article-elegant --ar 16:9 --quality 2k --json
```

结果：

- 失败。
- Tuzi API 返回 `Invalid Token`。
- 当前环境只检测到 `TUZI_API_KEY` / `TUZI_IMAGE_MODEL`，没有其他可用图像 provider 环境变量。

## 兜底产物

- 使用本地 SVG 生成可发布概念主图。
- 源文件：`images/x-agent-payment-permission-layer.svg`
- PNG：`images/x-agent-payment-permission-layer.png`

## 尝试 2：Codex 原生 image_gen

结果：

- 成功。
- 原始文件：`/Users/proerror/.codex/generated_images/019de182-abbd-7892-bad8-121ef711296c/ig_0192fae4d97794800169f4211cfecc81918fd853c326ac4284.png`
- 内容包副本：`images/x-agent-payment-permission-layer-codex.png`
- 尺寸：`1672x941`
- 该图已用于 X Article 草稿封面。

## 正文插图：Codex 原生 image_gen

1. `inline-01-final-step.png`
   - 原始文件：`/Users/proerror/.codex/generated_images/019de182-abbd-7892-bad8-121ef711296c/ig_0192fae4d97794800169f4232c99c8819187e473c364decff0.png`
   - 插入位置：`为什么 Agent 会卡在付款这一步` 之后
   - 作用：解释 Agent 卡在付款最后一步。
2. `inline-02-two-rails.png`
   - 原始文件：`/Users/proerror/.codex/generated_images/019de182-abbd-7892-bad8-121ef711296c/ig_0192fae4d97794800169f4235b392881918c0dbd701320ecae.png`
   - 插入位置：`两者真正的差别` 之后
   - 作用：解释 Stripe / OKX 两条支付轨道。
3. `inline-03-bounded-agent.png`
   - 原始文件：`/Users/proerror/.codex/generated_images/019de182-abbd-7892-bad8-121ef711296c/ig_0192fae4d97794800169f42392018c81918d30b0072e89f836.png`
   - 插入位置：`企业不会把钱交给 AI，但会把流程交给有边界的 Agent` 之后
   - 作用：解释企业接受的是有边界、可审批、可审计的 Agent。

## X Article 草稿插图状态

- 内容包正文已插入 3 张中间图。
- X Article 草稿箱现已确认包含封面 + 3 张正文图，未发布。
- 修复后的插入方式不是 AppleScript 粘贴，也不是 data image HTML，而是 X Article 正文工具栏路径：聚焦正文编辑器 -> `插入 / 添加媒体内容` -> 菜单项 `媒体` -> CDP file chooser。
- 当前草稿 preview URL：`https://x.com/compose/articles/edit/2050103269012484096/preview`。
- DOM 回读：`pbs.twimg.com/media` 大图 4 张，其中封面 `1672x669`，三张正文图均为 `1672x941`。
- 保存 payload 回读：`ArticleEntityUpdateContent` 最终包含 3 个正文 `atomic` blocks 和 3 个 `MEDIA` entity。

## 风格说明

- 16:9 主图
- 简洁 editorial tech 风格
- 不使用 Stripe / OKX logo
- 不使用真实银行卡号、密集 UI 或 crypto 金币堆

## 2026-05-01 X Article 正文图诊断补充

- 图片文件本身可用，且 X media upload pipeline 能接收这些 PNG。
- 旧 trace 显示问题发生在 X Article 内容保存层：此前误点封面/header 的媒体按钮时，上传成功的 media 没有进入正文 Article content。
- 新 trace `x-article-body-menu-20260501-try2` 显示正文图的正确入口是 `插入 / 添加媒体内容` -> `媒体`；走该路径后 media 会进入 `content_state.entity_map` 并在 preview 中持久化。
